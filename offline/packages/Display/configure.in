AC_INIT(configure.in)

AM_INIT_AUTOMAKE(pheve, 1.00)
AC_PROG_CXX(CC g++)
AC_ENABLE_STATIC(NO)
AC_PROG_LIBTOOL

if test $ac_cv_prog_gcc = yes; then
  CXXFLAGS="$CXXFLAGS -Wall -Werror"
fi

AC_OUTPUT( \
	   Makefile \
	   interface/Makefile \
	   display/Makefile \
	   modules/Makefile )
