#include "PgPostHelper.h"

#include <phool/PHPointerList.h>

#include <algorithm>
#include <cctype>
#include <string>

using namespace std;

std::string getTableName(const char* bankName)
  {
    string tablename(bankName);
    return tablename;
  }
